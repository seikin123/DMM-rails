<h1>投稿一覧</h1>
<% @lists.each do |list| %>
  <p>タイトル</p>
  <span>
    <%= link_to list.title, todolist_path(list.id) %>
  </span>
  <%= attachment_image_tag list, :image, format: 'jpeg', size: "100x100" %>
<% end %>